To login test account, tap button on the bottom of welcome screen "从备份恢复密钥", then select "keystore.whoops" file(inside the zip file), if password filed prompted, directly tap "OK", this backup keystore file was NO need of password.

:)
